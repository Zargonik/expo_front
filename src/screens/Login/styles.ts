import { StyleSheet } from 'react-native';

export const getStyles = () => StyleSheet.create({
    background: {
        width: "100%",
        height: "100%",
    },
    container: {
        height: "100%",
        justifyContent: "flex-end",
        paddingBottom: 30,
    },
    LoginForm: {
      
    }
});
